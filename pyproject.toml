[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "task-tracker"
version = "0.1.0"

[tool.setuptools]
py-modules = ["app"]

[project.scripts]
task-tracker = "app:main"